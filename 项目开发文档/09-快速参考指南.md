# 云盘系统 - 快速参考指南

> 快速查找关键信息和常用命令

## 📋 目录
- [关键配置](#关键配置)
- [常用命令](#常用命令)
- [核心API速查](#核心api速查)
- [数据库表速查](#数据库表速查)
- [错误码速查](#错误码速查)
- [性能指标](#性能指标)
- [技术选型对比](#技术选型对比)

## 🔧 关键配置

### 后端配置 (application.yml)

```yaml
# 核心配置项
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_disk
    username: root
    password: your_password
  
  redis:
    host: localhost
    port: 6379
    password: your_redis_password

jwt:
  secret: your_jwt_secret_key
  expiration: 7200000  # 2小时

storage:
  type: local  # local, aliyun_oss, tencent_cos
  max-file-size: 104857600  # 100MB
```

### 前端配置 (.env)

```bash
# API地址
VITE_API_BASE_URL=http://localhost:8080/api

# 文件上传限制
VITE_MAX_FILE_SIZE=104857600  # 100MB

# 分片大小
VITE_CHUNK_SIZE=5242880  # 5MB
```

### Nginx配置要点

```nginx
# 文件上传大小
client_max_body_size 500M;

# 超时设置
proxy_connect_timeout 300;
proxy_send_timeout 300;
proxy_read_timeout 300;

# 反向代理
location /api/ {
    proxy_pass http://backend:8080/api/;
}
```

## 💻 常用命令

### Docker命令

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f [service_name]

# 停止服务
docker-compose down

# 重启服务
docker-compose restart [service_name]

# 查看运行状态
docker-compose ps

# 进入容器
docker exec -it cloud-disk-backend bash

# 清理未使用的镜像
docker system prune -a
```

### Maven命令

```bash
# 编译
mvn clean compile

# 打包
mvn clean package

# 跳过测试打包
mvn clean package -DskipTests

# 运行测试
mvn test

# 运行应用
mvn spring-boot:run

# 查看依赖树
mvn dependency:tree

# 更新依赖
mvn versions:use-latest-releases
```

### NPM/PNPM命令

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 运行测试
pnpm test

# 代码检查
pnpm lint

# 代码格式化
pnpm format

# 预览构建结果
pnpm preview
```

### Git命令

```bash
# 创建功能分支
git checkout -b feature/your-feature develop

# 提交代码
git add .
git commit -m "feat(module): description"

# 推送代码
git push origin feature/your-feature

# 合并分支
git checkout develop
git merge --no-ff feature/your-feature

# 查看状态
git status

# 查看提交历史
git log --oneline --graph

# 撤销修改
git checkout -- file
git reset HEAD file
```

### MySQL命令

```bash
# 登录
mysql -u root -p

# 创建数据库
CREATE DATABASE cloud_disk;

# 选择数据库
USE cloud_disk;

# 导入数据
mysql -u root -p cloud_disk < init.sql

# 导出数据
mysqldump -u root -p cloud_disk > backup.sql

# 查看表结构
DESCRIBE table_name;

# 查看索引
SHOW INDEX FROM table_name;

# 查看表大小
SELECT 
  table_name,
  ROUND(((data_length + index_length) / 1024 / 1024), 2) AS 'Size (MB)'
FROM information_schema.TABLES
WHERE table_schema = 'cloud_disk';
```

### Redis命令

```bash
# 连接Redis
redis-cli -h localhost -p 6379 -a password

# 查看所有键
KEYS *

# 获取值
GET key

# 设置值
SET key value

# 删除键
DEL key

# 查看键类型
TYPE key

# 设置过期时间
EXPIRE key seconds

# 查看过期时间
TTL key

# 清空数据库
FLUSHDB

# 查看内存使用
INFO memory
```

## 🔌 核心API速查

### 认证相关

| 接口 | 方法 | 路径 | 说明 |
|-----|------|-----|------|
| 用户登录 | POST | /api/auth/login | 返回access_token和refresh_token |
| 用户注册 | POST | /api/auth/register | 需要邮箱验证码 |
| 退出登录 | POST | /api/auth/logout | 清除token |
| 刷新Token | POST | /api/auth/refresh | 使用refresh_token获取新的access_token |
| 发送验证码 | POST | /api/auth/send-code | 发送邮箱验证码 |

### 文件管理

| 接口 | 方法 | 路径 | 说明 |
|-----|------|-----|------|
| 获取文件列表 | GET | /api/files | 支持分页和排序 |
| 获取文件详情 | GET | /api/files/{id} | 返回文件完整信息 |
| 上传文件 | POST | /api/files/upload | multipart/form-data |
| 秒传检查 | POST | /api/files/sec-upload | 基于MD5检查 |
| 下载文件 | GET | /api/files/download/{id} | 返回文件流 |
| 删除文件 | DELETE | /api/files/{id} | 移入回收站 |
| 重命名文件 | PUT | /api/files/{id}/rename | 修改文件名 |
| 移动文件 | PUT | /api/files/{id}/move | 移动到其他文件夹 |
| 搜索文件 | GET | /api/files/search | 关键词搜索 |
| 创建文件夹 | POST | /api/files/folder | 新建文件夹 |

### 分片上传

| 接口 | 方法 | 路径 | 说明 |
|-----|------|-----|------|
| 初始化 | POST | /api/files/chunk/init | 返回uploadId |
| 上传分片 | POST | /api/files/chunk/upload | 上传单个分片 |
| 合并分片 | POST | /api/files/chunk/merge | 完成上传 |

### 文件分享

| 接口 | 方法 | 路径 | 说明 |
|-----|------|-----|------|
| 创建分享 | POST | /api/share | 生成分享链接 |
| 获取分享信息 | GET | /api/share/{code} | 需要密码验证 |
| 取消分享 | DELETE | /api/share/{id} | 删除分享 |
| 我的分享 | GET | /api/share/my | 分享列表 |

## 📊 数据库表速查

### 核心表

| 表名 | 说明 | 主要字段 | 索引 |
|-----|------|---------|------|
| sys_user | 用户表 | id, username, password, email | username, email |
| file_info | 文件信息表 | id, file_name, file_path, user_id | user_id, parent_id, file_md5 |
| file_share | 分享记录表 | id, share_code, file_id, user_id | share_code, file_id |
| storage_config | 存储源配置表 | id, storage_type, endpoint | storage_type |
| file_operation_log | 操作日志表 | id, user_id, operation_type | user_id, create_time |
| file_recycle | 回收站表 | id, file_id, delete_time | file_id, expire_time |

### 关键SQL

```sql
-- 查询用户文件列表
SELECT * FROM file_info 
WHERE user_id = ? AND parent_id = ? AND deleted = 0
ORDER BY create_time DESC;

-- 查询文件存储使用情况
SELECT user_id, SUM(file_size) as total_size 
FROM file_info 
WHERE deleted = 0 
GROUP BY user_id;

-- 查询最近上传的文件
SELECT * FROM file_info 
WHERE user_id = ? AND deleted = 0
ORDER BY create_time DESC 
LIMIT 10;

-- 清理过期分享
DELETE FROM file_share 
WHERE expire_time < NOW() AND expire_time IS NOT NULL;

-- 清理过期回收站文件
DELETE FROM file_recycle 
WHERE expire_time < NOW();
```

## ⚠️ 错误码速查

### 通用错误 (1000-1999)

| 错误码 | 说明 | 解决方案 |
|--------|------|---------|
| 1001 | 用户名已存在 | 更换用户名 |
| 1002 | 邮箱已被注册 | 更换邮箱或找回密码 |
| 1003 | 用户名或密码错误 | 检查登录信息 |
| 1004 | Token已过期 | 刷新Token |
| 1005 | Token无效 | 重新登录 |

### 文件错误 (2000-2999)

| 错误码 | 说明 | 解决方案 |
|--------|------|---------|
| 2001 | 文件不存在 | 检查文件ID |
| 2002 | 文件名重复 | 修改文件名 |
| 2003 | 存储空间不足 | 清理文件或联系管理员 |
| 2004 | 文件类型不允许 | 检查允许的文件类型 |
| 2005 | 文件大小超限 | 压缩文件或分片上传 |
| 2006 | 文件上传失败 | 重试或检查网络 |

### 分享错误 (3000-3999)

| 错误码 | 说明 | 解决方案 |
|--------|------|---------|
| 3001 | 分享不存在 | 检查分享链接 |
| 3002 | 分享已过期 | 联系分享者 |
| 3003 | 分享密码错误 | 检查密码 |
| 3004 | 下载次数已用完 | 联系分享者 |

### 权限错误 (4000-4999)

| 错误码 | 说明 | 解决方案 |
|--------|------|---------|
| 4001 | 无权限访问 | 检查权限设置 |
| 4002 | 操作被禁止 | 联系管理员 |

## 📈 性能指标

### 响应时间目标

| 操作 | 目标时间 | 可接受范围 |
|-----|---------|-----------|
| 登录 | <200ms | <500ms |
| 文件列表 | <100ms | <300ms |
| 文件上传 | 取决于文件大小和网络 | - |
| 文件下载 | 取决于文件大小和网络 | - |
| 文件删除 | <100ms | <200ms |
| 搜索 | <300ms | <500ms |

### 并发能力

| 指标 | 目标值 | 说明 |
|-----|--------|------|
| 并发用户 | 1000+ | 同时在线用户数 |
| QPS | 500+ | 每秒查询数 |
| 上传并发 | 100+ | 同时上传用户数 |
| 数据库连接池 | 100 | 最大连接数 |

### 存储容量

| 指标 | 默认值 | 可配置 |
|-----|--------|--------|
| 单用户配额 | 10GB | ✅ |
| 单文件大小 | 100MB | ✅ |
| 总存储容量 | 1TB+ | ✅ |

## 🔍 技术选型对比

### 后端框架对比

| 特性 | Spring Boot | Django | Express.js |
|-----|------------|--------|-----------|
| 语言 | Java | Python | Node.js |
| 性能 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| 生态 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 学习曲线 | 中等 | 简单 | 简单 |
| 企业级支持 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| **选择原因** | Java生态成熟，企业级应用首选 | - | - |

### 前端框架对比

| 特性 | Vue 3 | React | Angular |
|-----|-------|-------|---------|
| 学习曲线 | 简单 | 中等 | 较难 |
| 性能 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 生态 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| TypeScript | ✅ | ✅ | ✅ |
| 社区活跃度 | 高 | 最高 | 高 |
| **选择原因** | 渐进式框架，易学易用，性能优秀 | - | - |

### 数据库对比

| 特性 | MySQL | PostgreSQL | MongoDB |
|-----|-------|-----------|---------|
| 类型 | 关系型 | 关系型 | 文档型 |
| 性能 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| ACID | ✅ | ✅ | 部分支持 |
| 扩展性 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 成熟度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **选择原因** | 成熟稳定，生态完善，企业首选 | - | - |

### 缓存方案对比

| 特性 | Redis | Memcached | Ehcache |
|-----|-------|-----------|---------|
| 数据类型 | 丰富 | 简单 | 简单 |
| 持久化 | ✅ | ❌ | ❌ |
| 分布式 | ✅ | ✅ | ✅ |
| 性能 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 功能 | 丰富 | 简单 | 中等 |
| **选择原因** | 功能强大，支持多种数据结构 | - | - |

## 🎯 开发检查清单

### 功能开发前
- [ ] 需求明确
- [ ] 设计评审通过
- [ ] 数据库表设计
- [ ] API接口设计
- [ ] 前端原型确认

### 代码提交前
- [ ] 代码自测通过
- [ ] 单元测试通过
- [ ] 代码规范检查通过
- [ ] 无明显bug
- [ ] 代码注释完整
- [ ] Git提交信息规范

### 发布上线前
- [ ] 集成测试通过
- [ ] 性能测试通过
- [ ] 安全测试通过
- [ ] 数据库备份
- [ ] 回滚方案准备
- [ ] 监控告警配置
- [ ] 文档更新

## 🔧 故障排查速查

### 应用无法启动

```bash
# 1. 检查端口占用
netstat -nltp | grep 8080

# 2. 查看日志
tail -f logs/application.log

# 3. 检查配置文件
cat application.yml

# 4. 检查Java进程
ps aux | grep java
```

### 数据库连接失败

```bash
# 1. 测试连接
mysql -h localhost -u root -p

# 2. 检查MySQL状态
systemctl status mysql

# 3. 查看错误日志
tail -f /var/log/mysql/error.log

# 4. 检查防火墙
ufw status
```

### Redis连接失败

```bash
# 1. 测试连接
redis-cli ping

# 2. 检查Redis状态
systemctl status redis

# 3. 查看配置
cat /etc/redis/redis.conf

# 4. 检查端口
netstat -nltp | grep 6379
```

### 文件上传失败

```bash
# 1. 检查磁盘空间
df -h

# 2. 检查文件权限
ls -la /data/upload

# 3. 检查Nginx配置
nginx -t

# 4. 查看Nginx日志
tail -f /var/log/nginx/error.log
```

## 📞 常用链接

### 开发文档
- [项目概述](./01-项目概述.md)
- [API文档](./04-API接口文档.md)
- [开发指南](./08-开发指南与最佳实践.md)

### 在线工具
- Swagger UI: http://localhost:8080/doc.html
- Actuator: http://localhost:8080/api/actuator/health
- Prometheus: http://localhost:9090
- Grafana: http://localhost:3000

### 官方文档
- Spring Boot: https://spring.io/projects/spring-boot
- Vue 3: https://vuejs.org/
- Element Plus: https://element-plus.org/
- MyBatis-Plus: https://baomidou.com/

## 💡 常见问题 FAQ

**Q: 如何修改上传文件大小限制？**
A: 修改 `application.yml` 中的 `spring.servlet.multipart.max-file-size` 和 Nginx 的 `client_max_body_size`

**Q: 如何添加新的存储源？**
A: 实现 `StorageService` 接口，并在 `StorageServiceFactory` 中注册

**Q: 如何自定义错误码？**
A: 在 `ErrorCodeEnum` 中添加新的错误码定义

**Q: 如何启用HTTPS？**
A: 配置SSL证书到Nginx，并修改前端API地址为HTTPS

**Q: 如何进行数据迁移？**
A: 使用 `mysqldump` 导出数据，然后在新环境导入

---

**快速参考指南持续更新中...**

*最后更新: 2025-01-01*

